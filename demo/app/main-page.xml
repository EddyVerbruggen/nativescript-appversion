<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">
    <ActionBar title="My App" icon="" />

    <StackLayout class="p-20">
        <Label text="{{ 'Version name sync : ' + version_name_sync }}" class="h1 text-center" />
        <Label text="{{ 'Version name : ' + version_name }}" class="h1 text-center" />
        <Label text="{{ 'APP ID sync : ' + app_id_sync }}" class="h1 text-center" />
        <Label text="{{ 'APP ID : ' + app_id }}" class="h1 text-center" />
    </StackLayout>
</Page>
